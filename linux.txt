{
    "shell_cmd": "g++ -O2 \"${file}\" -o \"${file_base_name}\" && ./\"${file_base_name}\" < \"${file_path}/input.txt\" > \"${file_path}/output.txt\"",
    "working_dir": "${file_path}",
    "selector": "source.c++, source.cpp"
}